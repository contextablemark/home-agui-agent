{
    "config": {
        "step": {
            "user": {
                "title": "Configure AG-UI Agent",
                "description": "Connect to a remote [AG-UI]({ag_ui_url}) compatible agent endpoint.",
                "data": {
                    "agui_endpoint": "AG-UI Endpoint URL",
                    "timeout": "Request Timeout (seconds)",
                    "bearer_token": "Bearer Token"
                },
                "data_description": {
                    "agui_endpoint": "The URL of your AG-UI compatible agent server (e.g., http://localhost:8005/agent)",
                    "timeout": "Maximum time to wait for agent responses",
                    "bearer_token": "Optional authentication token for the AG-UI endpoint"
                }
            }
        },
        "error": {
            "invalid_url": "Invalid URL format. Must start with http:// or https://",
            "cannot_connect": "Unable to connect to the AG-UI endpoint. Please verify the URL is correct and the server is running.",
            "unknown": "An unexpected error occurred."
        },
        "abort": {
            "already_configured": "This AG-UI endpoint is already configured."
        }
    }
}
